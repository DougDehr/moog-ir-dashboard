# Copy this file to .streamlit/secrets.toml (not committed to git) and fill in
# your own contact email. SEC EDGAR requires a descriptive User-Agent with a
# contact string on every request; this is not sent anywhere except sec.gov.

SEC_CONTACT_EMAIL = "you@example.com"
